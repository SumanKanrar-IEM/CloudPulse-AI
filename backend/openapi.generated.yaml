openapi: 3.1.0
info:
  title: CloudPulse AI API
  description: 'Serverless cloud governance, cost, and compliance platform for AWS.


    Every endpoint except `/health` requires a Cognito-issued bearer token. The caller''s

    role is derived from directory group membership on every request and is never
    stored by

    the platform (FR-031a). A token whose group claim contains zero or more than one
    mapped

    group is refused (FR-032a).

    '
  version: 0.1.0
paths:
  /health:
    get:
      tags:
      - system
      summary: Service and dependency health
      description: Report this service's health and that of its dependencies.
      operationId: getHealth
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          description: At least one dependency is unreachable (FR-042)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '500':
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      security: []
  /me:
    get:
      tags:
      - identity
      summary: The signed-in caller and their resolved role
      description: "Any caller with exactly one resolved role may read this, and no\
        \ one else.\n\nA caller mapping to zero or multiple directory groups never\
        \ reaches here \u2014\n`get_principal` raises 403 first (FR-032a)."
      operationId: getCurrentUser
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentUser'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '500':
          description: Unexpected server error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /accounts/external-id:
    post:
      tags:
      - accounts
      summary: Get a fresh platform-generated ExternalId ahead of cross-account registration
      description: T016a. Admin only, deliberately stateless -- see module and openapi.yaml
        docs.
      operationId: generateExternalId
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalIdResponse'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /accounts:
    get:
      tags:
      - accounts
      summary: List every registered account
      description: FR-010/FR-010a. All three roles may view.
      operationId: listAccounts
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountsList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      tags:
      - accounts
      summary: Register a new AWS account
      description: FR-006/FR-007/FR-011a. Verifies before accepting; refuses a duplicate
        (FR-009).
      operationId: registerAccount
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: The request conflicts with the account's current state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Request body or parameters failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /accounts/{account_id}:
    patch:
      tags:
      - accounts
      summary: Edit an account's scan-region list
      description: FR-008/FR-011a. Admin only. Takes effect from the next scan onward.
      operationId: updateAccountRegions
      parameters:
      - name: account_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Account Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccountUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /accounts/{account_id}/deactivate:
    post:
      tags:
      - accounts
      summary: Stop scanning an account without deleting its history
      description: 'FR-009a/FR-011a. A scan already running is unaffected (FR-009b)
        -- this only

        changes `cloud_account.status`, which the next scheduler pass reads, not the

        in-flight execution.'
      operationId: deactivateAccount
      parameters:
      - name: account_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Account Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /accounts/{account_id}/reactivate:
    post:
      tags:
      - accounts
      summary: Resume scanning a deactivated account
      description: 'FR-009c/FR-011a. No re-registration, no re-verification -- a stale
        role is

        caught by the same scan-failure handling that covers any active account

        (Edge Cases), on the next scan attempt.'
      operationId: reactivateAccount
      parameters:
      - name: account_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Account Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: The request conflicts with the account's current state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /accounts/{account_id}/scans:
    post:
      tags:
      - accounts
      summary: Trigger an on-demand scan
      description: 'FR-026/FR-026a (as amended by spec 004 FR-022). Admin or operator;
        viewer

        refused.'
      operationId: triggerScan
      parameters:
      - name: account_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Account Id
      responses:
        '202':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Scan'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: The request conflicts with the account's current state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    get:
      tags:
      - accounts
      summary: Retrieve an account's scan history
      description: 'FR-033. Any role may view, same as the accounts list itself (FR-010a)
        --

        read access here carries no more privilege than seeing an account exists.'
      operationId: listScanHistory
      parameters:
      - name: account_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Account Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScansList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /rules:
    get:
      tags:
      - rules
      summary: List every tagging rule
      description: 'FR-001-FR-004. All three roles may view (FR-030). One row per
        key -- the

        latest version, via `DISTINCT ON` ordered by version descending.'
      operationId: listRules
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RulesList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      tags:
      - rules
      summary: Create a new tagging rule
      description: 'FR-001. Admin only (FR-029). Refused if the key already exists
        -- editing an

        existing key is PATCH''s job (FR-006), not a second POST.'
      operationId: createRule
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuleCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Rule'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: A rule with this key is already registered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Request body or parameters failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /rules/{rule_key}:
    patch:
      tags:
      - rules
      summary: Edit a tagging rule
      description: 'FR-005/FR-006. Admin only. Creates a new version under the same
        key

        (research.md R-301) rather than mutating the existing version in place --
        the

        edit takes effect starting with the next scan that begins after this call,
        never

        a scan already in progress (Clarifications session 2026-08-25).'
      operationId: updateRule
      parameters:
      - name: rule_key
        in: path
        required: true
        schema:
          type: string
          title: Rule Key
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuleUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Rule'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Request body or parameters failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /sdas:
    get:
      tags:
      - sdas
      summary: List every registered SDA
      description: FR-007. Any role may view (FR-030).
      operationId: listSdas
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SdasList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      tags:
      - sdas
      summary: Register a new SDA
      description: 'FR-007. Admin only (FR-029). Refused with 409 if the mapping overlaps
        an

        existing SDA''s (FR-010a).'
      operationId: registerSda
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SdaCreate'
        required: true
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sda'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: This tag-value mapping overlaps an already-registered SDA's
            mapping.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Request body or parameters failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /sdas/{sda_id}:
    patch:
      tags:
      - sdas
      summary: Edit an SDA's tag-value mapping
      description: 'FR-010. Admin only. Reclassification of matching resources happens
        at the

        next scan (Phase 7''s worker calls `sda_matching.reclassify_account_resources`),

        not here -- this endpoint only updates the SDA row itself.'
      operationId: updateSda
      parameters:
      - name: sda_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Sda Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SdaUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Sda'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '409':
          description: This tag-value mapping overlaps an already-registered SDA's
            mapping.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - sdas
      summary: Remove an SDA
      description: 'FR-010b. Admin only. Never refused for having attached resources
        -- the

        `ON DELETE SET NULL` foreign key is what immediately reverts every attached

        resource to the "No SDA" bucket, not application code, and not deferred to

        the next scan (Acceptance Scenario US2.5).'
      operationId: removeSda
      parameters:
      - name: sda_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Sda Id
      responses:
        '204':
          description: Successful Response
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /sdas/unmatched-resources:
    get:
      tags:
      - sdas
      summary: List resources in the "No SDA" bucket
      description: 'FR-009/FR-012. Any role may view (FR-030) -- the P1 API the P2
        triage

        screen (FR-012) consumes; this endpoint itself is P1 since FR-009''s

        visibility requirement is P1.'
      operationId: listUnmatchedResources
      parameters:
      - name: accountId
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Accountid
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnmatchedResourcesList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /findings:
    get:
      tags:
      - findings
      summary: List findings
      description: FR-014. Any role may view (FR-030). Most recently opened first.
      operationId: listFindings
      parameters:
      - name: accountId
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Accountid
      - name: resourceId
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Resourceid
      - name: status
        in: query
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/FindingStatus'
          - type: 'null'
          title: Status
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FindingsList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /findings/{finding_id}/acknowledge:
    post:
      tags:
      - findings
      summary: Acknowledge an open finding
      description: 'FR-015-FR-017, FR-020, FR-028. Admin or operator only. Never touches

        `status` or any compliance score (FR-017). Idempotent: the `WHERE

        acknowledged_at IS NULL` guard means a near-simultaneous second attempt

        updates nothing rather than racing or duplicating (data-model.md).'
      operationId: acknowledgeFinding
      parameters:
      - name: finding_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Finding Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FindingAcknowledgment'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /findings/{finding_id}/suggestion:
    get:
      tags:
      - findings
      summary: A finding's remediation suggestion, if one exists
      description: 'FR-018, FR-019. Any role may view. No suggestion yet is a normal
        200

        with empty fields, not a 404 -- only a missing finding is a 404.'
      operationId: getFindingSuggestion
      parameters:
      - name: finding_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Finding Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemediationSuggestion'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - findings
      summary: Attach a demo/QA test suggestion to a finding
      description: 'FR-020a, FR-028a. Admin only. Always writes `source=admin_seeded`
        --

        there is no parameter here that can produce `ai_generated`.'
      operationId: setFindingSuggestionSeed
      parameters:
      - name: finding_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Finding Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemediationSuggestionSeed'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemediationSuggestion'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /findings/{finding_id}/notifications:
    get:
      tags:
      - findings
      summary: Every notification attempt recorded for a finding
      description: 'FR-013''s auditable trail. Any role may view, matching this router''s

        other read endpoints. A finding nothing has been attempted for is a normal

        200 with an empty list -- only a missing finding is a 404, the same

        distinction `getFindingSuggestion` already draws.'
      operationId: listFindingNotifications
      parameters:
      - name: finding_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Finding Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationAttempts'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /resources:
    get:
      tags:
      - resources
      summary: List the resource inventory, paged and filtered
      description: 'FR-010, FR-011. Any role may view (FR-030). Excludes soft-deleted

        resources by default (FR-030''s own "never a row deletion" note -- a

        deleted resource has no governance action to take, but its detail view

        stays reachable directly by ID, data-model.md).'
      operationId: listResources
      parameters:
      - name: accountId
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Accountid
      - name: service
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Service
      - name: region
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Region
      - name: sdaId
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Sdaid
      - name: tagStatus
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Tagstatus
      - name: ownerStatus
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Ownerstatus
      - name: page
        in: query
        required: false
        schema:
          type: integer
          minimum: 1
          default: 1
          title: Page
      - name: pageSize
        in: query
        required: false
        schema:
          type: integer
          maximum: 200
          minimum: 1
          default: 50
          title: Pagesize
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourcesPage'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Request body or parameters failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /resources/{resource_id}:
    get:
      tags:
      - resources
      summary: One resource's tags, owner + evidence, findings, and enrichment detail
      description: 'FR-012. Any role may view (FR-030). Reachable even for a soft-deleted

        resource (data-model.md) -- only the default listing excludes those.'
      operationId: getResourceDetail
      parameters:
      - name: resource_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Resource Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceDetail'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /accounts/{account_id}/compliance-score:
    get:
      tags:
      - compliance
      summary: Retrieve an account's compliance score
      description: FR-018/FR-019/FR-019a. Any role may view (FR-030).
      operationId: getAccountComplianceScore
      parameters:
      - name: account_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Account Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComplianceScore'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /sdas/{sda_id}/compliance-score:
    get:
      tags:
      - compliance
      summary: Retrieve one SDA's compliance score
      description: FR-018/FR-019/FR-019a, scoped to one SDA's resources only.
      operationId: getSdaComplianceScore
      parameters:
      - name: sda_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Sda Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComplianceScore'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /resources/{resource_id}/owner:
    get:
      tags:
      - ownership
      summary: Retrieve a resource's attributed owner
      description: 'FR-020-FR-028. Any role may view (FR-030). 200 with a null `ownerEmail`/

        `confidence` (not 404) when the resource exists but is queued

        unattributed (FR-022) -- a resource genuinely not found still 404s.'
      operationId: getResourceOwner
      parameters:
      - name: resource_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Resource Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceOwnership'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '404':
          description: Target record does not exist, or the caller is not entitled
            to know that it does.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /owner-identity-pattern:
    get:
      tags:
      - ownership
      summary: Retrieve the configured owner-identity resolution pattern
      description: FR-028. Any role may view.
      operationId: getOwnerIdentityPattern
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OwnerIdentityPatternBody'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      tags:
      - ownership
      summary: Set the owner-identity resolution pattern
      description: FR-028. Admin only (FR-029) -- takes effect immediately, no redeploy.
      operationId: setOwnerIdentityPattern
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OwnerIdentityPatternBody'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OwnerIdentityPatternBody'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Request body or parameters failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /owner-identity-overrides:
    get:
      tags:
      - ownership
      summary: List manual owner-identity overrides
      description: FR-027. Any role may view.
      operationId: listOwnerIdentityOverrides
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OwnerIdentityOverridesList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    put:
      tags:
      - ownership
      summary: Create or replace a manual owner-identity override
      description: 'FR-027. Admin only (FR-029). Upserts on `(tenant_id, principal_id)`
        --

        consulted last in the resolution chain, after the owner tag and pattern.'
      operationId: setOwnerIdentityOverride
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OwnerIdentityOverrideBody'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OwnerIdentityOverrideBody'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Request body or parameters failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /spend:
    get:
      tags:
      - spend
      summary: Daily spend records, optionally filtered
      description: 'FR-001, FR-003. `sdaId=none` filters to the "No SDA" bucket specifically

        -- distinct from omitting the parameter, which applies no SDA filter at

        all.'
      operationId: listSpend
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
          title: From
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
          title: To
      - name: accountId
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Accountid
      - name: sdaId
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Sdaid
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpendList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Request body or parameters failed validation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /spend/summary:
    get:
      tags:
      - spend
      summary: Org-wide spend totals with trend, for the cost dashboard
      description: 'FR-003, SC-002. Org-wide (no account filter) -- every registered

        account''s spend, rolled up.


        A trend day is `null` if *any* account has a gap for that day (FR-002a) --

        a conservative "this total is not the whole picture" signal, since a

        partial sum presented as complete would be worse than an honest null.'
      operationId: getSpendSummary
      parameters:
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date
          title: From
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date
          title: To
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpendSummary'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /budgets:
    get:
      tags:
      - budgets
      summary: Every project's budget and threshold state
      description: 'FR-015. Any role may read. Ordered by project name, so the dashboard''s

        budget rows line up with its spend-by-project table rather than arriving in

        insertion order.'
      operationId: listBudgets
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetsList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /budget-overruns:
    get:
      tags:
      - budget-overruns
      summary: Findings opened by a project exceeding its budget
      description: 'FR-016. Most recently opened first, matching `GET /findings`''s
        ordering

        so the two lists read as one workbench rather than two conventions.'
      operationId: listBudgetOverruns
      parameters:
      - name: status
        in: query
        required: false
        schema:
          anyOf:
          - $ref: '#/components/schemas/FindingStatus'
          - type: 'null'
          title: Status
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BudgetOverrunsList'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /utilization:
    get:
      tags:
      - utilization
      summary: Utilization overall, per account, and per project
      description: 'FR-018. One response carries all three levels, which is what makes
        the

        account-to-project-to-resource drill-down reachable in three steps without

        a request per expansion.


        `accountId` narrows every level to that account -- `overall` included, so

        the headline figure and the account row agree rather than the headline

        silently staying tenant-wide.'
      operationId: getUtilization
      parameters:
      - name: accountId
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Accountid
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UtilizationReport'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /iam-hygiene:
    get:
      tags:
      - iam-hygiene
      summary: Roles, users, and keys that appear unused
      description: 'FR-019. Active flags by default; `includeCleared=true` returns
        the

        history too, so an admin can see that a principal was flagged and later

        became active again rather than wondering where a flag went.'
      operationId: listIamHygieneFlags
      parameters:
      - name: accountId
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: uuid
          - type: 'null'
          title: Accountid
      - name: includeCleared
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Includecleared
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IamHygieneFlags'
        '401':
          description: No valid identity presented.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '403':
          description: Valid identity, but the caller's role does not permit the action
            -- or the caller maps to zero or multiple directory groups and therefore
            has no resolvable role (FR-032a, FR-034).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
components:
  schemas:
    Account:
      properties:
        id:
          type: string
          title: Id
        alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Alias
        connectionMode:
          type: string
          title: Connectionmode
        awsAccountId:
          type: string
          title: Awsaccountid
        roleArn:
          anyOf:
          - type: string
          - type: 'null'
          title: Rolearn
        scanRegions:
          items:
            type: string
          type: array
          title: Scanregions
        status:
          type: string
          title: Status
        lastScan:
          anyOf:
          - $ref: '#/components/schemas/ScanSummary'
          - type: 'null'
        createdAt:
          type: string
          format: date-time
          title: Createdat
      type: object
      required:
      - id
      - connectionMode
      - awsAccountId
      - scanRegions
      - status
      - createdAt
      title: Account
    AccountCreate:
      properties:
        alias:
          anyOf:
          - type: string
          - type: 'null'
          title: Alias
        connectionMode:
          $ref: '#/components/schemas/ConnectionMode'
        awsAccountId:
          anyOf:
          - type: string
          - type: 'null'
          title: Awsaccountid
        roleArn:
          anyOf:
          - type: string
          - type: 'null'
          title: Rolearn
        externalId:
          anyOf:
          - type: string
          - type: 'null'
          title: Externalid
        scanRegions:
          items:
            type: string
          type: array
          title: Scanregions
      additionalProperties: false
      type: object
      required:
      - connectionMode
      title: AccountCreate
      description: FR-006. `extra="forbid"` is FR-001's structural refusal of access-key
        input.
    AccountUpdate:
      properties:
        scanRegions:
          items:
            type: string
          type: array
          minItems: 1
          title: Scanregions
      additionalProperties: false
      type: object
      required:
      - scanRegions
      title: AccountUpdate
    AccountUtilization:
      properties:
        accountId:
          type: string
          title: Accountid
        alias:
          type: string
          title: Alias
        utilization:
          $ref: '#/components/schemas/UtilizationFigure'
      type: object
      required:
      - accountId
      - alias
      - utilization
      title: AccountUtilization
    AccountsList:
      properties:
        accounts:
          items:
            $ref: '#/components/schemas/Account'
          type: array
          title: Accounts
      type: object
      required:
      - accounts
      title: AccountsList
    Budget:
      properties:
        id:
          type: string
          title: Id
        sdaId:
          type: string
          title: Sdaid
        sdaName:
          type: string
          title: Sdaname
        amountUsd:
          type: string
          pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
          title: Amountusd
        actual80CrossedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Actual80Crossedat
        actual100CrossedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Actual100Crossedat
        forecast80CrossedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Forecast80Crossedat
        forecast100CrossedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Forecast100Crossedat
      type: object
      required:
      - id
      - sdaId
      - sdaName
      - amountUsd
      title: Budget
      description: 'One project''s guardrail and which thresholds it has crossed.


        All four crossed timestamps are exposed, not just the one that opens a

        finding: FR-015 makes 80% dashboard-visible precisely *because* it sends no

        notification, so hiding it here would leave the only warning signal with

        nowhere to appear.'
    BudgetOverrun:
      properties:
        id:
          type: string
          title: Id
        sdaId:
          type: string
          title: Sdaid
        sdaName:
          type: string
          title: Sdaname
        budgetUsd:
          anyOf:
          - type: string
          - type: 'null'
          title: Budgetusd
        severity:
          type: string
          title: Severity
        status:
          type: string
          title: Status
        openedAt:
          type: string
          format: date-time
          title: Openedat
        resolvedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Resolvedat
        acknowledgedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Acknowledgedat
        escalatedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Escalatedat
      type: object
      required:
      - id
      - sdaId
      - sdaName
      - severity
      - status
      - openedAt
      title: BudgetOverrun
      description: 'One overrun finding, carrying the project it is about.


        The same lifecycle fields a tag-violation finding exposes -- this is one

        `Finding` row, not a parallel entity, and it moves through exactly the same

        open/acknowledge/resolve/suppress states.'
    BudgetOverrunsList:
      properties:
        overruns:
          items:
            $ref: '#/components/schemas/BudgetOverrun'
          type: array
          title: Overruns
      type: object
      required:
      - overruns
      title: BudgetOverrunsList
    BudgetsList:
      properties:
        budgets:
          items:
            $ref: '#/components/schemas/Budget'
          type: array
          title: Budgets
      type: object
      required:
      - budgets
      title: BudgetsList
    ComplianceScore:
      properties:
        compliantCount:
          type: integer
          title: Compliantcount
        totalCount:
          type: integer
          title: Totalcount
        score:
          type: number
          title: Score
      type: object
      required:
      - compliantCount
      - totalCount
      - score
      title: ComplianceScore
    ConnectionMode:
      type: string
      enum:
      - local
      - assume_role
      title: ConnectionMode
      description: 'FR-031-adjacent: how the platform reaches a scanned account.


        Both modes are roles-only. There is deliberately no mode that accepts an access

        key -- Principle III forbids storing one, so it is absent from the vocabulary

        rather than merely discouraged.'
    CurrentUser:
      properties:
        userId:
          type: string
          title: Userid
        email:
          type: string
          title: Email
        displayName:
          anyOf:
          - type: string
          - type: 'null'
          title: Displayname
        role:
          type: string
          title: Role
          description: Derived from directory group membership; never stored by the
            platform.
        tenantId:
          type: string
          title: Tenantid
      type: object
      required:
      - userId
      - email
      - role
      - tenantId
      title: CurrentUser
    DependencyCheck:
      properties:
        name:
          type: string
          title: Name
        status:
          type: string
          enum:
          - healthy
          - unhealthy
          title: Status
        detailMessage:
          anyOf:
          - type: string
          - type: 'null'
          title: Detailmessage
          description: Never contains credentials, connection strings, or secrets
            (FR-046).
      type: object
      required:
      - name
      - status
      title: DependencyCheck
    ErrorBody:
      properties:
        code:
          type: string
          title: Code
          description: Stable and machine-readable. SCREAMING_SNAKE_CASE.
        message:
          type: string
          title: Message
          description: Human-readable and safe to display. Never reveals whether a
            given record exists to a caller not entitled to know (FR-035).
        correlationId:
          type: string
          title: Correlationid
          description: Matches the id in the structured logs for this request (FR-044).
        details:
          anyOf:
          - items:
              $ref: '#/components/schemas/ErrorDetail'
            type: array
          - type: 'null'
          title: Details
          description: Field-level detail. Present for validation failures only.
      type: object
      required:
      - code
      - message
      - correlationId
      title: ErrorBody
    ErrorDetail:
      properties:
        field:
          type: string
          title: Field
        issue:
          type: string
          title: Issue
      type: object
      required:
      - field
      - issue
      title: ErrorDetail
      description: One field-level validation problem.
    ErrorEnvelope:
      properties:
        error:
          $ref: '#/components/schemas/ErrorBody'
      type: object
      required:
      - error
      title: ErrorEnvelope
      description: 'The uniform error envelope (FR-043).


        Declared as a model, not just built as a dict, so it appears in the **generated**

        OpenAPI document -- which FR-048 makes the binding contract. An envelope that
        only

        exists at runtime would be invisible to the generated client, leaving every

        consumer to hand-roll the error type.'
    ExternalIdResponse:
      properties:
        externalId:
          type: string
          title: Externalid
        cloudFormationTemplateUrl:
          type: string
          title: Cloudformationtemplateurl
      type: object
      required:
      - externalId
      - cloudFormationTemplateUrl
      title: ExternalIdResponse
    Finding:
      properties:
        id:
          type: string
          title: Id
        resource:
          $ref: '#/components/schemas/ResourceSummary'
        ruleKey:
          type: string
          title: Rulekey
        ruleVersion:
          type: integer
          title: Ruleversion
        severity:
          type: string
          title: Severity
        status:
          type: string
          title: Status
        openedAt:
          type: string
          format: date-time
          title: Openedat
        resolvedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Resolvedat
        acknowledgedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Acknowledgedat
        acknowledgedBy:
          anyOf:
          - type: string
          - type: 'null'
          title: Acknowledgedby
        escalatedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Escalatedat
      type: object
      required:
      - id
      - resource
      - ruleKey
      - ruleVersion
      - severity
      - status
      - openedAt
      title: Finding
    FindingAcknowledgment:
      properties:
        findingId:
          type: string
          title: Findingid
        acknowledgedAt:
          type: string
          format: date-time
          title: Acknowledgedat
        acknowledgedBy:
          type: string
          title: Acknowledgedby
      type: object
      required:
      - findingId
      - acknowledgedAt
      - acknowledgedBy
      title: FindingAcknowledgment
    FindingStatus:
      type: string
      enum:
      - open
      - resolved
      - suppressed
      title: FindingStatus
      description: 'Spec 003 owns the lifecycle semantics (FR-055).


        These members are the schema''s accommodation of them, not the authoritative

        definition. Spec 003 may extend this enum by additive migration without

        renegotiating the table.'
    FindingsList:
      properties:
        findings:
          items:
            $ref: '#/components/schemas/Finding'
          type: array
          title: Findings
      type: object
      required:
      - findings
      title: FindingsList
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    HealthResponse:
      properties:
        status:
          type: string
          enum:
          - healthy
          - unhealthy
          title: Status
        checks:
          items:
            $ref: '#/components/schemas/DependencyCheck'
          type: array
          title: Checks
        correlationId:
          type: string
          title: Correlationid
        version:
          anyOf:
          - type: string
          - type: 'null'
          title: Version
      type: object
      required:
      - status
      - checks
      - correlationId
      title: HealthResponse
    IamHygieneFlag:
      properties:
        id:
          type: string
          title: Id
        accountId:
          type: string
          title: Accountid
        principalType:
          type: string
          title: Principaltype
        principalIdentifier:
          type: string
          title: Principalidentifier
        evidence:
          additionalProperties: true
          type: object
          title: Evidence
        flaggedAt:
          type: string
          format: date-time
          title: Flaggedat
        clearedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Clearedat
      type: object
      required:
      - id
      - accountId
      - principalType
      - principalIdentifier
      - evidence
      - flaggedAt
      title: IamHygieneFlag
      description: 'One unused-principal recommendation, with the evidence behind
        it.


        `evidence` travels with every flag rather than being fetched separately:

        this is asking a human to delete something, and a recommendation without

        its basis is a guess presented as a fact.'
    IamHygieneFlags:
      properties:
        flags:
          items:
            $ref: '#/components/schemas/IamHygieneFlag'
          type: array
          title: Flags
      type: object
      required:
      - flags
      title: IamHygieneFlags
    InventoryResourceSummary:
      properties:
        id:
          type: string
          title: Id
        accountId:
          type: string
          title: Accountid
        arn:
          type: string
          title: Arn
        resourceType:
          type: string
          title: Resourcetype
        service:
          type: string
          title: Service
        region:
          type: string
          title: Region
        sdaId:
          anyOf:
          - type: string
          - type: 'null'
          title: Sdaid
        tagStatus:
          type: string
          title: Tagstatus
        ownerStatus:
          type: string
          title: Ownerstatus
      type: object
      required:
      - id
      - accountId
      - arn
      - resourceType
      - service
      - region
      - tagStatus
      - ownerStatus
      title: InventoryResourceSummary
    NotificationAttempt:
      properties:
        id:
          type: string
          title: Id
        cadencePoint:
          type: string
          title: Cadencepoint
        outcome:
          type: string
          title: Outcome
        recipientEmail:
          anyOf:
          - type: string
          - type: 'null'
          title: Recipientemail
        attemptedAt:
          type: string
          format: date-time
          title: Attemptedat
      type: object
      required:
      - id
      - cadencePoint
      - outcome
      - attemptedAt
      title: NotificationAttempt
      description: 'One recorded attempt, whatever its outcome (spec 005, FR-013).


        `recipientEmail` is null for every non-`sent` outcome -- there was no

        recipient, which is the point of recording the attempt at all.'
    NotificationAttempts:
      properties:
        findingId:
          type: string
          title: Findingid
        notifications:
          items:
            $ref: '#/components/schemas/NotificationAttempt'
          type: array
          title: Notifications
      type: object
      required:
      - findingId
      - notifications
      title: NotificationAttempts
    OwnerIdentityOverrideBody:
      properties:
        principalId:
          type: string
          title: Principalid
        ownerEmail:
          type: string
          title: Owneremail
      type: object
      required:
      - principalId
      - ownerEmail
      title: OwnerIdentityOverrideBody
    OwnerIdentityOverridesList:
      properties:
        overrides:
          items:
            $ref: '#/components/schemas/OwnerIdentityOverrideBody'
          type: array
          title: Overrides
      type: object
      required:
      - overrides
      title: OwnerIdentityOverridesList
    OwnerIdentityPatternBody:
      properties:
        pattern:
          anyOf:
          - type: string
          - type: 'null'
          title: Pattern
      type: object
      title: OwnerIdentityPatternBody
    OwnershipEvidence:
      properties:
        kind:
          type: string
          title: Kind
        cloudtrailEventId:
          anyOf:
          - type: string
          - type: 'null'
          title: Cloudtraileventid
        principal:
          anyOf:
          - type: string
          - type: 'null'
          title: Principal
        eventTime:
          anyOf:
          - type: string
          - type: 'null'
          title: Eventtime
      type: object
      required:
      - kind
      title: OwnershipEvidence
    ProjectUtilization:
      properties:
        sdaId:
          anyOf:
          - type: string
          - type: 'null'
          title: Sdaid
        sdaName:
          anyOf:
          - type: string
          - type: 'null'
          title: Sdaname
        utilization:
          $ref: '#/components/schemas/UtilizationFigure'
      type: object
      required:
      - sdaId
      - sdaName
      - utilization
      title: ProjectUtilization
    RemediationSuggestion:
      properties:
        findingId:
          type: string
          title: Findingid
        suggestionText:
          anyOf:
          - type: string
          - type: 'null'
          title: Suggestiontext
        blastRadiusNote:
          anyOf:
          - type: string
          - type: 'null'
          title: Blastradiusnote
        source:
          anyOf:
          - type: string
          - type: 'null'
          title: Source
      type: object
      required:
      - findingId
      title: RemediationSuggestion
    RemediationSuggestionSeed:
      properties:
        suggestionText:
          type: string
          title: Suggestiontext
        blastRadiusNote:
          type: string
          title: Blastradiusnote
      type: object
      required:
      - suggestionText
      - blastRadiusNote
      title: RemediationSuggestionSeed
    ResourceDetail:
      properties:
        id:
          type: string
          title: Id
        arn:
          type: string
          title: Arn
        resourceType:
          type: string
          title: Resourcetype
        service:
          type: string
          title: Service
        region:
          type: string
          title: Region
        tags:
          additionalProperties:
            type: string
          type: object
          title: Tags
        detail:
          additionalProperties: true
          type: object
          title: Detail
        owner:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Owner
        findings:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Findings
      type: object
      required:
      - id
      - arn
      - resourceType
      - service
      - region
      - tags
      - detail
      - findings
      title: ResourceDetail
    ResourceOwnership:
      properties:
        resourceId:
          type: string
          title: Resourceid
        ownerEmail:
          anyOf:
          - type: string
          - type: 'null'
          title: Owneremail
        confidence:
          anyOf:
          - type: string
          - type: 'null'
          title: Confidence
        evidence:
          anyOf:
          - $ref: '#/components/schemas/OwnershipEvidence'
          - type: 'null'
        attributedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Attributedat
      type: object
      required:
      - resourceId
      title: ResourceOwnership
    ResourceSummary:
      properties:
        id:
          type: string
          title: Id
        arn:
          type: string
          title: Arn
        resourceType:
          type: string
          title: Resourcetype
        region:
          type: string
          title: Region
        accountId:
          type: string
          title: Accountid
      type: object
      required:
      - id
      - arn
      - resourceType
      - region
      - accountId
      title: ResourceSummary
    ResourcesPage:
      properties:
        resources:
          items:
            $ref: '#/components/schemas/InventoryResourceSummary'
          type: array
          title: Resources
        page:
          type: integer
          title: Page
        pageSize:
          type: integer
          title: Pagesize
        totalCount:
          type: integer
          title: Totalcount
      type: object
      required:
      - resources
      - page
      - pageSize
      - totalCount
      title: ResourcesPage
    Rule:
      properties:
        id:
          type: string
          title: Id
        key:
          type: string
          title: Key
        version:
          type: integer
          title: Version
        enabled:
          type: boolean
          title: Enabled
        definition:
          $ref: '#/components/schemas/RuleDefinition'
        effectiveFrom:
          anyOf:
          - type: string
          - type: 'null'
          title: Effectivefrom
      type: object
      required:
      - id
      - key
      - version
      - enabled
      - definition
      title: Rule
    RuleCreate:
      properties:
        key:
          type: string
          title: Key
        definition:
          $ref: '#/components/schemas/RuleDefinition'
      additionalProperties: false
      type: object
      required:
      - key
      - definition
      title: RuleCreate
    RuleDefinition:
      properties:
        required:
          type: boolean
          title: Required
        allowedValues:
          anyOf:
          - items:
              type: string
            type: array
          - type: 'null'
          title: Allowedvalues
        formatPattern:
          anyOf:
          - type: string
          - type: 'null'
          title: Formatpattern
        severity:
          type: string
          enum:
          - low
          - medium
          - high
          - critical
          title: Severity
          default: medium
      additionalProperties: false
      type: object
      required:
      - required
      title: RuleDefinition
      description: 'FR-004: three independent checks a rule may express, any subset
        of them.'
    RuleUpdate:
      properties:
        definition:
          $ref: '#/components/schemas/RuleDefinition'
        enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
      additionalProperties: false
      type: object
      required:
      - definition
      title: RuleUpdate
      description: 'No `key` field, deliberately (FR-006): an edit targets a key via
        the URL

        path, never the request body.'
    RulesList:
      properties:
        rules:
          items:
            $ref: '#/components/schemas/Rule'
          type: array
          title: Rules
      type: object
      required:
      - rules
      title: RulesList
    Scan:
      properties:
        id:
          type: string
          title: Id
        accountId:
          type: string
          title: Accountid
        trigger:
          type: string
          title: Trigger
        status:
          type: string
          title: Status
        startedAt:
          type: string
          format: date-time
          title: Startedat
        finishedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Finishedat
        resourceCount:
          anyOf:
          - type: integer
          - type: 'null'
          title: Resourcecount
        added:
          anyOf:
          - type: integer
          - type: 'null'
          title: Added
          description: Resources first seen during this scan's window (spec 004, R-405).
        removed:
          anyOf:
          - type: integer
          - type: 'null'
          title: Removed
          description: Resources marked deleted during this scan's window (spec 004,
            R-405).
        changed:
          anyOf:
          - type: integer
          - type: 'null'
          title: Changed
          description: Pre-existing resources re-confirmed during this scan's window
            (R-405).
      type: object
      required:
      - id
      - accountId
      - trigger
      - status
      - startedAt
      title: Scan
    ScanSummary:
      properties:
        status:
          type: string
          title: Status
        startedAt:
          type: string
          format: date-time
          title: Startedat
        finishedAt:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Finishedat
        resourceCount:
          anyOf:
          - type: integer
          - type: 'null'
          title: Resourcecount
      type: object
      required:
      - status
      - startedAt
      title: ScanSummary
    ScansList:
      properties:
        scans:
          items:
            $ref: '#/components/schemas/Scan'
          type: array
          title: Scans
      type: object
      required:
      - scans
      title: ScansList
    Sda:
      properties:
        id:
          type: string
          title: Id
        name:
          type: string
          title: Name
        ownerEmail:
          type: string
          title: Owneremail
        team:
          anyOf:
          - type: string
          - type: 'null'
          title: Team
        tagValues:
          additionalProperties:
            type: string
          type: object
          title: Tagvalues
      type: object
      required:
      - id
      - name
      - ownerEmail
      - tagValues
      title: Sda
    SdaCreate:
      properties:
        name:
          type: string
          title: Name
        ownerEmail:
          type: string
          title: Owneremail
        team:
          anyOf:
          - type: string
          - type: 'null'
          title: Team
        tagValues:
          additionalProperties:
            type: string
          type: object
          title: Tagvalues
      additionalProperties: false
      type: object
      required:
      - name
      - ownerEmail
      - tagValues
      title: SdaCreate
    SdaUpdate:
      properties:
        ownerEmail:
          anyOf:
          - type: string
          - type: 'null'
          title: Owneremail
        team:
          anyOf:
          - type: string
          - type: 'null'
          title: Team
        tagValues:
          anyOf:
          - additionalProperties:
              type: string
            type: object
          - type: 'null'
          title: Tagvalues
      additionalProperties: false
      type: object
      title: SdaUpdate
    SdasList:
      properties:
        sdas:
          items:
            $ref: '#/components/schemas/Sda'
          type: array
          title: Sdas
      type: object
      required:
      - sdas
      title: SdasList
    SpendByProject:
      properties:
        sdaId:
          anyOf:
          - type: string
          - type: 'null'
          title: Sdaid
        sdaName:
          anyOf:
          - type: string
          - type: 'null'
          title: Sdaname
        totalUsd:
          type: string
          pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
          title: Totalusd
      type: object
      required:
      - totalUsd
      title: SpendByProject
    SpendList:
      properties:
        spend:
          items:
            $ref: '#/components/schemas/SpendRecord'
          type: array
          title: Spend
      type: object
      required:
      - spend
      title: SpendList
    SpendRecord:
      properties:
        accountId:
          type: string
          title: Accountid
        sdaId:
          anyOf:
          - type: string
          - type: 'null'
          title: Sdaid
        service:
          anyOf:
          - type: string
          - type: 'null'
          title: Service
        spendDate:
          type: string
          format: date
          title: Spenddate
        amountUsd:
          anyOf:
          - type: string
            pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
          - type: 'null'
          title: Amountusd
        isGap:
          type: boolean
          title: Isgap
      type: object
      required:
      - accountId
      - spendDate
      - isGap
      title: SpendRecord
    SpendSummary:
      properties:
        totalUsd:
          type: string
          pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
          title: Totalusd
        byProject:
          items:
            $ref: '#/components/schemas/SpendByProject'
          type: array
          title: Byproject
        trend:
          items:
            $ref: '#/components/schemas/SpendTrendPoint'
          type: array
          title: Trend
      type: object
      required:
      - totalUsd
      - byProject
      - trend
      title: SpendSummary
    SpendTrendPoint:
      properties:
        date:
          type: string
          format: date
          title: Date
        totalUsd:
          anyOf:
          - type: string
            pattern: ^(?!^[-+.]*$)[+-]?0*\d*\.?\d*$
          - type: 'null'
          title: Totalusd
      type: object
      required:
      - date
      title: SpendTrendPoint
    UnmatchedResourcesList:
      properties:
        resources:
          items:
            $ref: '#/components/schemas/ResourceSummary'
          type: array
          title: Resources
      type: object
      required:
      - resources
      title: UnmatchedResourcesList
    UtilizationFigure:
      properties:
        used:
          type: integer
          title: Used
        provisioned:
          type: integer
          title: Provisioned
        percent:
          anyOf:
          - type: number
          - type: 'null'
          title: Percent
      type: object
      required:
      - used
      - provisioned
      - percent
      title: UtilizationFigure
      description: 'A utilization percentage and the population it was measured over.


        `percent` is null, never 0 or 100, when nothing measurable exists -- and

        `used`/`provisioned` are always present so a caller can state the scope

        ("2 of 4 enriched resources") rather than implying a claim over the whole

        inventory (R-509).'
    UtilizationReport:
      properties:
        overall:
          $ref: '#/components/schemas/UtilizationFigure'
        byAccount:
          items:
            $ref: '#/components/schemas/AccountUtilization'
          type: array
          title: Byaccount
        byProject:
          items:
            $ref: '#/components/schemas/ProjectUtilization'
          type: array
          title: Byproject
      type: object
      required:
      - overall
      - byAccount
      - byProject
      title: UtilizationReport
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
  securitySchemes:
    cognitoJwt:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Cognito-issued access token. Validated by the API Gateway JWT authorizer,
        then re-validated for role cardinality by the application (research.md R-004).
security:
- cognitoJwt: []
